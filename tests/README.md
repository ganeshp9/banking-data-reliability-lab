# Tests

Tests will be added with each implementation milestone rather than after the
pipeline is complete. Planned suites include unit, data-contract, integration,
idempotency, reconciliation, and recovery tests.
